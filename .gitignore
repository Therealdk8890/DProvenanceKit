.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Generated sample trace database (produced by the GenerateSample executable)
sample_traces.sqlite
# WebVisualizer export written by the FoundationModelsRegressionDemo executable
/fm-regression.json
# SQLite WAL sidecar files — never commit these
*.sqlite-wal
*.sqlite-shm

# Root-level scratch experiments (not part of any build target)
/test.swift
/test_loop.swift
/run_test.swift

# Internal go-to-market / sales ops material — local only, never publish
/go-to-market/
